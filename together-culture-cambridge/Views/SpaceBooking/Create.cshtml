@model together_culture_cambridge.Models.SpaceBooking

@{
    ViewData["Title"] = "Create";
}

<h1>Create</h1>

<h4>SpaceBooking</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="EndUserId" class="control-label"></label>
                <select asp-for="EndUserId" class ="form-control" asp-items="ViewBag.EndUserId"></select>
            </div>
            <div class="form-group">
                <label asp-for="SpaceId" class="control-label"></label>
                <select asp-for="SpaceId" class ="form-control" asp-items="ViewBag.SpaceId"></select>
            </div>
            <div class="form-group">
                <label asp-for="BookingDate" class="control-label"></label>
                <input asp-for="BookingDate" class="form-control" />
                <span asp-validation-for="BookingDate" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
